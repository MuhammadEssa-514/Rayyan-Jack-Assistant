# Jack AI Voice Engine - Python Dependencies
# Install: pip install -r requirements.txt

# Wake word detection
openwakeword>=0.6.0

# Speech recognition (Faster Whisper - fast offline STT)
faster-whisper>=1.0.0

# Audio capture
pyaudio>=0.2.13
sounddevice>=0.4.6
numpy>=1.24.0

# WebSocket client (connect to Jack AI server)
python-socketio[client]>=5.10.0
aiohttp>=3.9.0

# HTTP client (send commands to server)
requests>=2.31.0
httpx>=0.25.0

# Text-to-speech (Piper TTS)
piper-tts>=1.2.0

# Audio playback for TTS
playsound>=1.3.0
pygame>=2.5.0

# Async support
asyncio-mqtt>=0.16.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
